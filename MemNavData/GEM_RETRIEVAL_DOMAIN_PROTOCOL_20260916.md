# Frozen archive-access comparison, 2026-09-16

Population: the same 13 histories in 12 scenes used by the completed main-table control-attribution experiment. Selection uses historical support >= 0.5, support over the last seven historical decision observations < 0.1, and an initial goal-overlap upper bound < 0.1. This is an existing metadata subgroup, not a new independent benchmark or proof that every task requires old memory.

Each history executes two arms on the same physical A100: full eligible historical retrieval, and retrieval limited to exactly the last seven historical decision observations. All seven raw-frame indices come from the frozen trace's original decision steps. The original causal writer processes all historical RGB in both arms. The experiment changes the set of archive entries eligible for the certificate shortlist before temporal suppression; it does not reset, shorten, compress, or otherwise change the geometry state. Learned raw retrieval diagnostics may still be computed by the legacy backend, but the frozen control route consumes only the certificate shortlist, whose entire path into sparse localization is checked.

The full-history arm delegates directly to the original shortlist implementation. The recent arm uses the same DINO scores, top-k, temporal gap, and tie-breaking rule with a seven-entry eligibility mask. A frozen query session cannot change its domain or shortlist. Every sparse localization must consume exactly that shortlist. The original SuperPoint/LightGlue/PnP, strict certificate, canonical reference depth, window32 LingBot geometry, NavDP, original ImageGoal, deterministic diffusion seeds, 600-action budget, turning interface and 1 m automatic arrival criterion are preserved. Valid certificate rejection retains native execution; runtime or evidence errors invalidate execution and are not navigation failures.

The original 13 full-GEM outcomes remain reproducibility references. Both arms are executed as a matched pair because geometry and controller inputs are checked at the tensor/pose level on the same physical GPU. This adds 26 rollouts, not 26 independent tasks. The first history is the original longest, 564-frame case, and its two rollouts remain part of the total. Launch the remaining 12 only after this first pair passes the full execution audit. Do not retry completed valid method outcomes.

Validation includes one-to-one replay frame indexing; exact last-seven decision IDs; identical RGB and geometric pose streams through the first query frame; identical initial monocular depth; identical full DINO score vectors before domain restriction; an independent reconstruction of shortlist ranking and temporal suppression; immutable cached candidates; actual downstream selected-anchor membership; no evaluator pose/depth/motion inputs to memory; reconstructed actions/path length/success; and exact full-reference success, action count, path length, SPL and turn count.

Primary outcome: paired navigation success for full versus recent-only archive access, with exact paired discordances and scene-cluster uncertainty. Also report accepted recall, support age, initial and trajectory bearing error, and executed actions. Report this contrast separately from the three-control attribution family, since it addresses archive access. The two related mechanistic studies together do not prove long-distance navigation, autonomous STOP, or a particular learned-memory mechanism. Recent seven decision observations are not recent seven raw frames or recent64.

All 13 scheduled histories and unfavorable outcomes remain in the report. Preserve infrastructure failures and retry only exact failed cells after recording the cause. Seven CPU tests cover eligibility before suppression, native full-history parity, immutable caches, domain bypass, goal binding and invalid/future indices. GPU model validation is supplied by the first formal pair.
