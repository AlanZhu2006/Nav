# 共视 0.1–1.0 分层：最新状态

## 2026-09-10 终局（北京时间 01:02 核验）

159/159 配对、477条导航全部完成，17240716最终汇总通过；没有待补失败项。
五档 native/raw/GEM 分别为7/22/13（n=23）、11/26/20（n=27）、
7/28/25（n=28）、5/28/25（n=28）、8/23/22（n=25）；
原 Novel 为6/5/6（n=28），GEM 28/28 exact native。
支持查询中24条 raw-only 成功有23条 GEM 未接管；最低档对 raw 的 Holm p=0.0586，
不能把中途观察或原始 p 当作通过多重比较后的确认。
另一个新 actual-mono A 总体也已90/90完成，但不属于本共视总体。
完整数值、SPL、配对与证据路径见 [终局报告](REPAIRED_HM3D_FINAL_RESULTS_20260910.md)。
以下保留当时的运行记录，不代表当前队列状态。

## 19:52 更新（北京时间）

`17240715`：**140/159 配对任务已完整归档**，未见终止失败。
140份归档均有完整三臂独立验证和成员回读记录，未见失败/不完整归档。
总量仍为477个三臂rollout，不改区间或目标。
这不是最终SR；`17240716`仍等待全部导航结束后独立汇总。

14:22临时将后续新启动的并发上限降到2；14:54因A/构造仍未推进，再降到1，
为同QOS的新实际mono-A留出调度机会。原有运行任务没有中断。
当时实际父数组已回读确认`ArrayTaskThrottle=1`，调整时task57/58没有被中断。
15:00共视task58运行，新A的scene rank2/3也已在ga024/ga031运行。
首轮A/构造完成后已恢复原上限4。新查询首项等待期间临时降为2，
`17262832_0`启动后再次恢复4并回读确认；所有已有运行均未中断。
等待原因包括JobArrayTaskLimit和QOSGrpGRES，
不能将这种资源排队误报为实验失败。

另行提交的新实际mono-A是`17253441`，对应离线构造`17253442`，不属于本共视总体。
其首前缀112条A/28非空scenes已全部完成，88条到达；全部独立验证与归档回读通过。
首scene的0 pair损耗保留；全部28个scene构造已结束，45对查询覆盖23个scene，
90个实际目标已封存并通过输入检查。新query首项`17262832_0`已完整通过独立验证与归档，
剩余89项为`17263230`，其独立CPU汇总为`17263231`。
新full-mono目前累计47/90项完整归档（含首项）；19:51回读时剩余项全部等待
QOSGrpGRES，暂无运行单元。并发上限4、1小时时限均未改变；
它没有被暂停或取消，不能把排队当作环境故障。两批最终汇总均尚未生成。
不能把旧A上的新版query分层与该新完整A流程混为一个主实验。

下文保留早期启动记录与冻结设计。存储最新已核验值见HPC清理文档，不沿用初始98%文件数占用。

## 早期启动记录

北京时间 2026-09-09 08:52 左右核查。构造作业 **17222616** 的 28 项全部
COMPLETED；CPU 封存 **17222953** 也已完成。159 个目标全部通过输入独立核验。

**三臂导航已启动：17240715，159 个任务、477 条 rollout。**
index 0、1 已完成，用时分别为8分04秒、13分05秒；当时index 2、3在A100运行，其余等待GPU配额。
index 0 的完整三臂结果：native 0、raw-fixed 1、CEC 1；独立动作/深度/终点 SPL 校验通过，
完整原始归档 148,144,179 bytes，逐文件回读校验通过。这只是首个配对目标，
不是总体效应，不能据此调参数或提前宣布分层结论。
后续 CPU 汇总 **17240716** 用 afterany 依赖，并要求全部配对与归档校验完成；
缺任务时只输出缺失清单，不把基础设施失败混为导航失败。

## 已封存的实际总体

| 目标组 | histories | scenes | 实际共视范围 |
|---|---:|---:|---:|
| [0.1,0.3) | 23 | 17 | 0.1123–0.2924 |
| [0.3,0.5) | 27 | 20 | 0.3082–0.4979 |
| [0.5,0.7) | 28 | 21 | 0.5050–0.6992 |
| [0.7,0.9) | 28 | 21 | 0.7040–0.8861 |
| [0.9,1.0] | 25 | 19 | 0.9074–1.0000 |
| 原始 Natural Novel 对照 | 28 | 21 | 0–0.1167 |

主分层 131 目标，原始 Novel 对照 28 目标；20 条历史有全部五档。
缺格是不可构造，不是导航失败；不再补样。history 22 / vd3HHTEpmyA 的原 Novel
重标注 q=0.11667666466706658，保留原目标和标签并单列说明，不能称所有对照 q<0.1。
population SHA256：`182f3a6d2519d5b2c178b88345db4d0bb678088dd88487cfecaad9814c6fdaaf`。

## 固定设计

- 主区间：[0.1,0.3)、[0.3,0.5)、[0.5,0.7)、[0.7,0.9)、[0.9,1]。
- 28 条已冻结 actual-mono A histories，21 个 HM3D scenes；不是 expert 轨迹。
- 每条历史固定导航起点、目标物理位置、相机和历史，只改变目标图 yaw。
- 72 个固定方向；按预定义哈希选择各档目标，不按 CEC/DINO/导航成绩选择。
- 共视是目标表面在 frame 39 起的全部历史中的最大单向可见比例；另存 all-history 值。
- 使用实际 Habitat 内参和浮点深度做离线注释；不把 GT 发给控制器。
- 之后每个目标完整三臂配对：native、raw-fixed memory、CEC；证书阈值不变。
- 原 Natural Novel 是单独对照，不计入上述五档。
- 本实验重新执行修复接口下的 query 阶段；A 本身仍来自旧执行版本，不宣称全链路重采。

设计上限为 420 次分层主查询 rollout + 84 次 Novel 对照；最终实际为 393 + 84 = 477。
空档必须报告，尤其不能复制历史 JPEG、平移目标或放宽边界来凑 0.9–1.0。

## 已验证

本机与远端分别通过同一组 12 项 CPU 测试：区间边界、重复图排除、固定选择、
遮挡、前后方向、历史支持范围和超过 PNG 深度上限的浮点数据。
本机实际 Habitat 空场景的 color/depth projection matrix 相符，确认
`fx=355.8146095, fy=355.8146060`；这不是导航 SR 测试。
shell 语法检查、共享 SSH、远端文件哈希、safe_sbatch lint/test-only 均通过。
模型、共享环境、论文、真机未修改；没有 commit/push。

新查询入口额外通过本地/远端各 8 项测试、Novel/Revisit × 三臂共六种 CLI 检查；
只替换独立查询加载入口，模型与控制仍来自 c8cf8c60e7efd55f，测量 callback 与已通过
N=2 集成的版本逐字一致。完整终点、动作、规划、RGB/深度栅格及转身收据全部保留。

## 资源与产物

构造：A100，28 array elements，最多 4 并发；每任务 4 CPU、16 GB、1 h。
只启动 Habitat，不载入 NavDP/LingBot。CPU 封存阶段 1 CPU、2 GB、10 min。
scratch 文件数配额已使用约 98%；本阶段仅保存入选目标和紧凑扫描收据，
不复制历史。正式导航 A100 / 10 CPU / 96 GB / 1 h，最多 4 并发。
节点临时盘保存原始证据；逐臂独立复算、完整三臂校验后归档并回读每个文件哈希。
归档保留原始绝对路径，通过容器 bind 恢复路径后可重验，不改写历史收据。

远端根目录：
`/scratch/yz11502/Research/Nav-axis-uturn-results/hm3d_covisibility_repaired_20260909/run_bfb8fc3d5a1bb081`

- `construction/history_XX/construction.json`：固定历史的入选目标与缺失档。
- `construction/history_XX/candidate_scan.json`：全部 72 个方向的共视曲线。
- `construction/population.json`：全部构造完成后产生的冻结任务清单。
- 首臂终点/SPL 已记录；每项目三臂完成后独立校验并归档，总体统计仍待全部完成。

## 下一步

检查已提交三臂评测的首项实际终局、归档与独立校验，再按冻结总体汇总。
不按导航结果修改区间、增加历史或替换失败目标。

- [完整协议](HM3D_COVISIBILITY_REPAIRED_PROTOCOL_20260909.md)
- [提交收据](HM3D_COVISIBILITY_REPAIRED_SUBMISSION_20260909.json)
- [导航提交收据](HM3D_COVISIBILITY_REPAIRED_EVAL_SUBMISSION_20260909.json)
- [构造与封存代码](build_hm3d_covisibility_repaired.py)
