# 两历史实际图像构造探针：1/2，不是导航SR

2026-09-09 13:20（北京时间）。GPU job `17251134`：COMPLETED 0:0，5分08秒。
23项原容器前置测试通过；两个scene的全部历史RGB重渲染哈希与原实际A一致。
没有启动NavDP/LingBot，没有执行任何query导航。

| 实际修复版A历史 | Revisit共视 / 距离 | 合法Novel | 选择 | 完整pair |
|---|---|---|---|---|
| rJhMRvNn4DS / episode_0000 | 0.720343 / 2.529098m | front/side/rear均无 | 无 | 否 |
| jgPBycuV1Jq / episode_0000 | 0.720098 / 2.532646m | rear：共视0.042157、距离8.073555m | rear | 是 |

rJh的front和rear分别有98和57次候选走到视觉支持检查，全部被共视条件拒绝；side
没有候选走到这一阶段。每层最多5000次，阈值和目标yaw规则没有改。
这些是155次候选检查，不是155个独立导航episode，也不是穷举证明绝对不存在Novel。
空间筛选有候选，不代表图像未被历史观察过；先前CPU空间计数不能替代这次视觉证据。

## 原冻结门槛与任务状态

原协议约定两条都形成pair才自动启动首scene采集。结果只有1条，因此没有通过。
CPU续接 `17251808` 在2秒内 FAILED 1:0，具体异常为
`ValueError: Both declared pairs are required`；它先核验了probe源码/协议/目标文件，
然后在门槛处退出，**没有提交A、没有提交query**。
这不是又一次依赖损坏，不重装环境，不原样重试这个续接器。

保留原两条历史、全部失败计数及旧2/2门槛，不改写为通过。
结果反驳的是“固定方向是这两条无法配对的唯一原因”；不证明GEM无效，也不足以估计
196个新实际A任务的合法pair比例。

## 明确分开的后续开发检查

原自动续接停止后，仍需检验正式采集/构造入口在完整源scene上是否工作。
新建一个独立run root，执行原源顺序rank0的**全部四条A**，再按相同规则构造。
本次改变的是开发检查的启动条件，不是把旧2/2门槛降低并宣布通过：

- 原构造探针与自动续接仍记录未通过；没有开启原正式扩样链。
- 数据、scene/episode顺序、seed、模型、执行器、0.10/0.55–0.90支持与2–9m条件全部不变。
- 不挑仅有合法pair的jgP取代原首scene，不根据A/query SR筛选任务。
- 只运行一个scene的四条A和对应离线构造，检验成功、失败及无pair的完整保存。
- 不用“必须产生pair”作为这个开发检查的通过条件；否则又把合法的数据损耗当成代码错误。
  必须四条A正常终止、逐动作独立验证、无损归档与构造的全部源覆盖成立。
- 这不是新的独立确认，也不是已经提交全部源前缀。全量扩样/查询仍需该入口的实际证据。
- 不能把本次1/2解释为足够统计功效，或据此承诺目标24 histories一定能达到。

这样保留原失败结果，同时验证当前真正缺失的“实际A→完整构造损耗”管线。
若后续改变构造规则或总体，必须另立新协议，不沿用旧总体名或拼接新旧结果。

## 原始依据

根目录：
`/scratch/yz11502/Research/Nav-axis-uturn-results/repaired_fullmono_design_20260909/construct_b9297569ff0805d7`

- `summary.json` SHA：`3cb3467a677fe3b57e26e456f3d5b3c64e7767385ebf9eff4a2308c1d0ffdea5`
- rJh construction SHA：`03f1a44a9183841d82269bf4e1a3708a719efe43eae1d802fe848c43326899ee`
- jgP construction SHA：`1ec463bab63bb3dfb5a4ded759f4b2dc40c650832a201a944029728c24d31c84`
- 原probe bundle：`b9297569ff0805d7`
- 原失败续接bundle：`34f015404387b7ad`

未读取新query SR，未修改论文正文、摘要、引言或表格数字。
