# GEM 项目整理与 Git 同步（2026-09-17）

本次归档 9 月 7 日以来的实现、协议、核验工具和结果记录，统一当前文档入口。
导航 main 同步到 `AlanZhu2006/Nav` 和 `glbreeze/Nav`；真机代码在独立的
`AlanZhu2006/MemNav-RealWorld` 提交。论文结果保持原报告口径。

## 实现

- GEM 模块集中管理流式写入、当前深度、历史档案与目标查询，Agent 保留兼容接口。
- 纳入支持像素档案、KV 存储研究实现、连续目标和资源测量工具。
- 纳入 RGB 传输、单目深度栅格、覆盖率消融和配对评测修复及已有回归检查。
- 真机接入 Survey 状态复用、显式存储配置和冻结请求延迟回放，生产存储默认保持原值。

入口见 [仓库指南](../docs/REPOSITORY_GUIDE.md)，证据见
[实验索引](../docs/EXPERIMENT_INDEX.md)。

## 实验状态

论文 bearing 消融保留作者确定的 53 条完整配对。另有一条已核验恢复记录继续归档，
不因 Git 同步改写 cutoff。13 条低近期重叠查询保留独立定义。
详见 [bearing 状态](GEM_BEARING_CONTINUATION_STATUS_20260917.md)。

该状态文件关于 SSH 不通和本机场景可用的段落是恢复时的快照。之后的 9 月 17 日
存储整理已通过共享 SSH 核对 HPC 的 88 份关键记录，并清理本地 Scene-N1 和大型数组。
恢复条件见 [存储说明](../docs/LOCAL_STORAGE.md)，不能照搬旧状态启动实验。

## 工作区

只保留一个开发工作区，论文和真机作为内部的独立仓库。源码和小型协议/结果进入 Git，
大型数组留在 `/data`；原始 RGB、轨迹、统计和权重保留。

9 月 7 日入口与索引保存为日期快照。新 README 不再把旧队列、分母和部署计划当作
最新状态。本次没有启动实验或更新机器人运行服务。
