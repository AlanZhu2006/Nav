# Table I 两项失败的精确补跑（2026-09-11）

范围：用户授权修复并重跑失败项。仅补 `17306820_110`、`17306820_158`，
不改变已经完成或仍在排队的其他单元，不修改论文、模型或 GEM 默认值。

## 已核验的故障

两项均为 A100 节点 ga033 上的 ExitCode 1:0，不是超时、显存不足或导入失败。
两项均在最后一个 Revisit-native 臂中止，前三臂已完成但不纳入完整配对统计。

| index | 方法/场景 | 最后一步异常 |
|---|---|---|
| 110 | MP3D / NavDP / history26 | 第 176 个执行动作后的 planning append 与客户端 JPEG 摘要不一致 |
| 158 | MP3D / ViNT / history32 | 第 248 个执行动作后的目标 JPEG 摘要不一致 |

原始日志来自 `task/logs/revisit_native.log`；不只查看外层进程报错。
110 的服务端和客户端记录都包含 frame index/image SHA，因此不能套用
2026-08-29「旧服务端缺少 transaction 字段」的同名错误解释。

158 在失败前后，服务端记录的解码目标像素 SHA 均为
`65c5cbddbb3d16a5c3453bc267674069aa8d197b1776cc09a98e89b5007ba41b`，
但 JPEG SHA 从连续一致的
`fd2d9e204272c0fd43b70ee6786df63ef63ec0c6e4abd91681297af0959427b1`
变成一次性的
`24b190375aad79f1c4196c5e677bbdf967107318e07f603c6f8929ffb508e6a1`。
这说明回执一致性异常确实出现，不能证明是模型换了目标，也不足以证明 ga033 硬件故障。
未记录完整失败 HTTP 请求体，因此现有材料不能唯一归因于编码、传输、内存或哈希计算。

## 选择的修复范围

采用**原样补跑**而非未经证实的算法修改：原封存 runtime 和 plan 原样重跑两项，
使用原 A100 分区正常调度。所有字节、帧、深度 transaction 和目标消费检查保留。
禁止把不一致摘要改成像素近似匹配放行，或在已经推进模型状态后静默重复单个 POST。
这不是宣称根因已修复；是否完成以完整四臂及独立 verifier 为准。

首次曾尝试 `--exclude=ga033`，但集群 `sbatch --test-only` 拒绝该参数，
未产生正式提交；原封存尝试及其 manifest 保留。相同配置不带该参数时检查通过。
正式补跑不绕过集群限制，也不声称已经避开 ga033 或完成硬件故障修复。

每项重新运行全部四臂，同机、同进程、原目标和随机种子。保持：

- original runtime SHA：`7b6bffa77891ef4f041508dc7dc31b40fb56b7e1885334a9e180a1f06d76e099`；
- plan SHA：`0964f52738e22f2d16b099196605ef2e9681f57afce20fbdb99b417d8b535622`；
- 两个角色、native/GEM 两臂、原顺序、600 ticks、horizon 8、1 m 评分；
- 所有权重、strict certificate、2.5 m residual、单目深度及修复执行器；
- A100 / 每项 1 GPU、10 CPU、96 GB、1 小时；两项最多并行 2 个。

## 数据保留与最终汇总

原运行目录：
`/scratch/yz11502/Research/Nav-axis-uturn-results/table1_repaired_20260910/run_7b6bffa77891ef4f`。

原失败归档保持原位：

- 110：291,003,204 bytes，SHA `73e3702177415702c4da27e0b750c1962888eb036a1c587767003f7531cf0d01`；
- 158：95,340,013 bytes，SHA `dd3e26a714efe33e0c95f81b00f8654a3492cdce170e41f644b5767ec80d865e`。

补跑写入新的 `exact_retry_20260911_*` 目录。`combined_evaluation/` 的 210 个链接
仅将 110/158 指向新结果，其他 208 项仍指向原目录；不复制改写成功记录，不移动失败材料。
仍在排队的原任务可以照常写入各自目录。

新 summary 等待原正式数组终止、精确补跑数组成功后运行。使用原汇总器，重新检查
全部单项 verifier、原始归档 SHA 和总体完整性，只有 210/210 才生成完整 SR/SPL。
旧 summary 保留原行为，不用改写其失败结果制造原数组全成功。

提交器：`submit_table1_exact_retry_20260911.sh`。提交前检查原失败状态、归档 SHA、
冻结源码、原始计划，以及两个 Slurm `--test-only`；提交后回读分区、时限和实际节点。
实际 job ID 和启动状态在提交后另记，本文本身不是已提交或已完成收据。
