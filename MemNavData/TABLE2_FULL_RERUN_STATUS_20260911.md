# Table II 完整重跑进度（2026-09-11）

## 用户修订后的当前状态

**23:39 CST 更新：**用户已确认“逐段共同发题、双方各自连续执行”。新共同采样器和
双臂协调器已实现，253 项测试通过。本机双臂并存时在 GEM A 出现显存不足，native A
已完成；该 partial 不计配对 SR。按手册完成远端原容器预检后，A100 80 GB 技术首例
`17374795` 已在 `ga016` 运行，时限 1 小时，只有原 source 0 的 NRR 双臂，非正式数组。
最新共同目标版本请看 `TABLE2_CONTINUOUS_COMMON_STATUS_20260911.md`，下方旧 v2/v3
状态均作为历史记录保留，不再代表当前连续实验的执行协议。

**最新任务定位修订：用户明确 Table II 主表应报告实际连续 multileg 整体效果。**
具体要求是同一起始任务总体逐段自然减少，A/B/C-Novel 的可设计难度尽量接近，
B/C-Revisit 沿用统一构造标准；不追求条件 SR 相同，不增加其他方法变量。
当前 v3 仍是共享前缀条件查询，不能据其构造审计通过就提交为端到端主表。
先保留已完成菜单和封存代码，下一步核对本方法 A→B→C 状态连续传递及共同任务构造。
详见 `TABLE2_CONTINUOUS_EXECUTION_ALIGNMENT_20260911.md`。

**22:48 CST 本机进展：**已真正实现自身状态连续执行，GEM 的一条开发 NRR 完成
`A→B→C`，native 在同一 A/B 上失败于 B。前向 NNN 的补充检查两臂均 A 成功、B 失败，
且 A/B 的逐帧 RGB/pose 完全一致，GEM 无接管；新目标传递、原 first40 尺度与跨段历史
连续性均已核对。另一条 GEM NRN 也已全部完成：152 / 92 / 287 ticks，C-Novel 的
36 次规划均未由 GEM 接管，完整自身链通过独立复核。结果属于本机开发，不混入正式 SR。
当前没有提交新的连续 Table II HPC 作业。正式共同目标构造仍需确定，不能把各臂单独
选择的后继目标直接作为同任务配对。最新详情：`TABLE2_CONTINUOUS_LOCAL_RESULT_20260911.md`。
本批私有 GPU 服务已正常退出；相关回归测试 225 passed。下一步共同发题设计详见
`TABLE2_CONTINUOUS_COMMON_TASK_DRAFT_20260911.md`，尚未实施为正式协议。

用户明确改为“Novel 不要回头，Revisit 可以回头”。v2 的 A 144 条结果和已执行 B 保留，
不将其混入新 v3。20:21 CST 核验：原待运行的 B 数组 `17367919`、C 选择续接
`17367920` 和后处理续接 `17367925` 已取消；已开始的 B 首项不强行中断。
下文 19:37 及更早的排队描述属于历史记录，不是 v3 正在运行的状态。

新 v3 只限制 A/B/C-Novel 的起始路线方向为 ±60°；其他规则、模型与修复版执行器不变。
20:42 CST 本机全量构造差分审计完成：144 个来源全部保留，97 个有合规前向 A，
47 个在原预算内无合规前向目标；新目标严格等于旧菜单的前向子集。278 项相关测试通过。
尚未提交新 HPC 任务，没有新 SR。
最新协议：`TABLE2_FORWARD_NOVEL_PROTOCOL_20260911.md`；
控制变量审计与真实检查结果：`TABLE2_FORWARD_ONLY_AUDIT_20260911.md`。

## v2 历史核验记录

最新核验（19:37 CST）：**144/144 个 A 已完整归档，A 成功 50/144（34.72%）**；
其中 38 个成功前缀可构造两类 B，B 的 76 个查询（152 次双臂 rollout）已自动提交、等待 GPU 配额。
索引 21 的离线碰撞复算故障已通过 CPU 补充验证恢复，
没有重跑导航、改分或更改 controller；原失败归档仍完整保留。
A 阶段曾按调度补充将并发上限从 2 提高至 4；B 当前仍按原提交并发 2，尚无 B/C 导航结果。
最终独立复算与报告的 CPU 续接作业也已提交。下文保留各阶段时间记录；
当前 A 总体已完成，B/C 尚未完成，不能将 A 采集结果称为 GEM 的配对效应。

## 1. 本轮在做什么

用户已将“按新设计完整重跑 Table II”设为今晚 goal。本轮不是继续复用旧 A 轨迹，
而是重新采集实际单目 A，随后运行 B/C 的 Novel–Revisit 分支和 native/GEM 配对。

正式协议：`TABLE2_FULL_RERUN_PROTOCOL_20260911.md`，版本 `table2_balanced_actual_mono_20260911_v2`。

```text
144 个预定 actual-mono A
    → 实际 A 历史
    → B-Novel / B-Revisit，各跑 native / GEM
    → 成功 native-B 的实际 A+B 历史，按前序 B 角色 50/50 取 C 前缀
    → C-Novel / C-Revisit，各跑 native / GEM
    → SR / 实际位移 SPL / 配对增损 / 按场景聚类统计
```

保留 A、B、C 各自的条件分母和构造损耗；不是 GEM 自主连续完成三段的 joint SR。
运行时不读取 Novel/Revisit 标签；不能用 GEM-B 替换失败的 native-B。

## 2. 已完成的本机构造与验证

- 采用前期全量离线容量审计中全部 **18 个候选 MP3D scenes**；该筛选未读取 query SR。
- 每场景声明 8 个新 A 查询，共 **144 个源**；同一物理 carrier 的复用已显式登记，统计按 scene 聚类。
- 只复用 PT1/gapfill 场景资产、起点和内参，**不把 expert 历史放入 memory**。
- 144 个 A 的目标菜单全部完成；144/144 都找到合规目标。
- 九个“距离 × 方向”格全局匹配后**各 16 条**：距离 `[2,4) / [4,6) / [6,9]`，方向 front / side / rear。
- 所选目标与当前初始 RGB 的共视全部 `<0.10`，最大值 **0.09306**。
- 本地独立 verifier 核查全部 144 个目标的来源、图像、内参、配额和不含角色的 runtime spec，`verified=true`。
- **232 项相关测试通过**；native/GEM 的新来源 CLI dry-run 均通过，没有运行导航。

旧实际历史上的构造接口检查（仅 consumed diagnostic，不计入正式 SR）：

| 实际前缀 | Novel 菜单格数 | Revisit 菜单格数 | 解释 |
|---|---:|---:|---|
| actual A → B | 3 | 1 | 两种 B 查询均可构造 |
| actual A + native Novel-B → C | 0 | 3 | 构造正常结束，但没有未见 C；必须保留为供给不足 |
| actual A + native Revisit-B → C | 2 | 2 | 两种 C 查询均可构造 |

这也说明“场景可能具备多个未见区域”并不保证**每条实际探索轨迹**后都仍可构造 C-Novel。
本轮不通过重采样 SR 高的轨迹或临时放宽共视来补齐这种缺口。

## 3. 与当前修复版 controller 的关系

逐文件对比实际正在运行的 `table2_multipart_8b559e821e8c9df6`：

- bounded pursuit / front-goal adapter：完全相同；
- GEM runtime / certificate contract：完全相同；
- monocular depth runtime / LingBot / NavDP 两个服务入口：完全相同；
- NavDP policy agent / policy network：完全相同。

仍是 `bounded_standard / rgb_v1 / source_rgb / heading_on`，600 ticks、horizon=8，
只用单目几何和安装高度先验；没有重新引入 GT 落点投影，也没有训练或更换 policy。

本轮修改主要是：

1. 共同的 Novel 采样、初始不可见条件，以及空白目标图拒绝；
2. 先生成候选菜单，再按总体距离/方向供给匹配；
3. A→B→C 的分阶段封存、分支隔离、50/50 C 来源；
4. 不可构造的 B 在 CPU 记录，不为“没有查询”的条目申请 GPU；
5. 修正旧 CLI 即使查询不使用 expert episode，仍提前读取其目录字段的问题。

## 4. HPC 提交

共享 SSH master 正常，用户 `yz11502`，入口 `alantorch`。未关闭或重置用户连接。

**预检与自动续接作业 `17357512` 已通过，并已提交 A 段任务。**

| 阶段 | Job ID | 内容 |
|---|---|---|
| 环境/CLI/资产预检 | `17357512` | Habitat、MemNav、NavDP imports / 五种 CLI / checkpoint / 144 个发布目标全部通过 |
| A 首项 | `17357680` | 首个实际单目 A 与 B 菜单构造，独立验证后放行其余 A |
| 其余 143 个 A | `17357681` | 同一冻结总体，依赖 A 首项完整归档 |
| B 选择与自动提交 | `17357682` | 等待 A 总体完成，汇总候选后选择并提交 B |
| 最终后处理的首个续接 | `17360494` | CPU，等待 A reducer 成功，随后衔接 B/C reducer 与独立复算、结果报告 |

该 CPU 作业先检查同一容器中的 imports、三阶段 CLI、资产、checkpoint、完整目标 payload，
通过后自动提交 A 首项和其余 A；后续 reducer 再依次选择/提交 B、C 和总汇总。
每阶段的实际查询首项作技术验收，其余项依赖它成功归档；验收不是要求导航必须成功。
GPU array element 为 A100、1 GPU、1 小时，并发上限 2，实际运行受 QOS 限制。

提交时：**新一轮尚无导航 SR**。预检通过和 A 队列已提交不等于 144 个 A 已完成，更不等于 Table II 已完成。
旧 8-source HM3D pilot 单独保留，不能与本轮 MP3D 结果混算。

### 封存标识

| 内容 | SHA-256 |
|---|---|
| 正式 plan | `8472cbb878e420f80d5efc2b39f1b3ab8ef893c3a80532ab06c48addd21a6e33` |
| source bundle receipt | `7a66e6a3e056afed96f6d724a0936c2551ff45e46b78ec9d9a7796639f0363b6` |
| payload receipt | `06d7273252fbaa126aed96689e4c03617553985c06628827a5d63ade2e6c2266` |

远端结果根目录：

```text
/scratch/yz11502/Research/Nav-axis-uturn-results/table2_full_rerun_20260911_v2
```

远端 source bundle：

```text
/scratch/yz11502/Research/Nav-axis-uturn-source-bundles/table2_full_7a66e6a3e056afed
```

本地构造、plan、验证、runtime 比较与对应封装：

```text
/home/asus/Research/Nav-graph-blind/.diagnostics/table2_full_rerun_20260911_v2
```

## 5. 后续验收

- 查 `17357680` 首项及 `A_submission.json`，确认实际 GPU 开始和成功归档。
- A 完整归档后查 `b_population.json`：供给、实际九格分布及初始可见性，而非只看 A 的 SR。
- B 完整后查 `c_population.json`：两类 native-B 来源严格 50/50、完整 actual A+B，不串分支。
- 所有查询按双臂完整性及末动作落点复算 SR/SPL；未接管查询核查与 native 逐动作一致。
- 报告真实 A/B/C 分母，不声称三个阶段原始 SR 必须相等；不把条件 C 说成三段 joint。
- 汇总后独立复算并更新总账。旧 Table II 和论文数字暂未改动，也未进行 Git push。

## 6. 12:48 CST 续查：独立复算已用真实归档验证

`17357512` 的 Slurm 状态为 `COMPLETED / 0:0`，耗时 3 分 35 秒。
`17357680_[0]` 仍为 `PENDING (QOSGrpGRES)`，Slurm 未提供预计启动时间；其余 A 和 reducer 正常等待依赖。
这不是环境报错，也没有重复提交。新正式总体仍无导航 SR。

新增只读后处理器 `MemNavData/independent_verify_table2_full.py`，与封存运行时分离，不改变已提交任务。
它不调用 rollout、构造器或原汇总器的指标函数，直接读取归档中的逐动作真实位置、最后落点和原始规划输出：

- 独立积分实际路径、复算 SR/SPL，检查末步位移没有遗漏；
- 核对无 GEM 接管时的原生规划与动作逐项一致；
- 核对 C 的 native-B 来源、A+B 图像/位姿序列、分支末态与 50/50 来源；
- 独立计算配对增损、exact McNemar 与场景聚类区间，再与正式汇总交叉验证。

已在 HPC 对旧 pilot 的 `A/task_000`、`B/task_000`、`B/task_001` 三份真实归档执行 `read_task`，全部通过。
其中包含无接管双臂完全一致，以及 GEM 接管后成功、native 失败两种情况。
旧 B-Novel 的实际路径为 15.0707008106 m，双臂 SPL 均为 0.3501144539；这是后处理器复算验证，**不是新正式成绩**。
新后处理器与 Table II 构造/编排的 39 项单元测试通过；随后扩大复查范围，
MemNav 环境的 134 项检查、Habitat 环境的 6 项集成检查全部通过。
其中涉及 Habitat 的一项起初误放在 MemNav 环境运行，因缺少 `quaternion` 失败；
切回已有 Habitat 环境后通过，没有安装新依赖或修改封存运行时。

后处理器 SHA-256：`533e5f4f04a7f3511fa6f5e5cef5b004e60d2d47dc105d2d4d46a00ce3ee6221`。
正式完整独立验证要等新 A/B/C 全部完成后运行，目前不能标记为已通过。

### 12:56 CST：最终报告生成环节

新增 `MemNavData/report_table2_full.py`，只在完整汇总与独立复算的 SHA 匹配后生成中文 Markdown。
输出 A/B/C 五行主结果、四种 B→C 组合、实际距离/方向分布、C 支持来源及构造损耗，
保留条件分母，不自动改写论文。报告器只需 Python 标准库，不增加 GPU 或模型依赖。
报告器和独立复算器的 11 项测试通过；测试中的数值仅为显式 synthetic fixtures，不是导航结果。

完整任务结束后的调用顺序（文件已存在时不覆盖）：

```bash
python MemNavData/independent_verify_table2_full.py \
  --plan RUN/plan.json --run RUN --summary RUN/paired_summary.json \
  --out RUN/full_independent_verification.json
python MemNavData/report_table2_full.py \
  --summary RUN/paired_summary.json --verification RUN/full_independent_verification.json \
  --out RUN/TABLE2_FULL_RESULT.md
```

队列原因进一步确认：`a100_tandon` 分区 QoS 的 GPU 总量上限为 60，当时运行作业合计占用 60。
`17357680` 的依赖为空、状态正常；没有用新任务替换它，也没有修改分区或冻结实验设置。

### 13:23 CST：新版 A 首项已获得 GPU

`17357680_0` 已在 `ga012` 运行，约 13:22 CST 启动；当前处于计算节点内的环境/模型预检。
此时 `A/task_000/preflight/habitat.log` 已生成，未见 Slurm stderr 错误。
尚未产出第一条导航 SR，不能把 GPU 开始计为 A 成功。
其余 143 条仍由 `17357681` 等待首项完整归档，reducer `17357682` 等待全部 A 完成。

### 13:29 CST：首项完整归档，导航失败但技术验收通过

Slurm：`17357680_0 COMPLETED / 0:0`，实际耗时 **5 分 15 秒**，节点 `ga012`。
来源 `1LXtFkjw3qL/episode_0000`，冻结的 A 目标属于 2–4 m / front 格，
初始测地距离 2.7507228851 m、初始共视 0。

只读独立复算器已直接读取该项原始归档，核对得到：

- `reached=0`，600 步预算耗尽；
- 实际路径 **21.1616372935 m**；
- 最终平面目标距离 **3.6806241929 m**；
- SPL **0**，末步动作和终点完整；
- `actual_A_failed`，按协议不从该源继续构造 B。

这是有效导航失败，不是环境或进程失败，也不会换目标重跑以得到成功。
当前仅 **1/144 个 A 完成、已完成项中 0/1 成功**，不能从单项推断总体 SR。
首项通过技术验收后，`17357681` 已从等待依赖转为等待 GPU，原 143 项继续执行；
没有更改样本、控制器或成功标准。

### 13:41 CST：前三项完成，正式 A→B 构造链跑通

后续数组已实际并行运行过两项。`17357681_1` 耗时 4 分 03 秒，
`17357681_2` 耗时 5 分 18 秒，均为 `COMPLETED / 0:0`。

| A index | 已执行步数 | 到达 | 实际路径 (m) | 最终距离 (m) | 两类 B 均可构造 |
|---:|---:|---:|---:|---:|---|
| 0 | 600 | 0 | 21.161637 | 3.680624 | 否：A 失败 |
| 1 | 173 | 1 | 5.066263 | 0.980819 | 是 |
| 2 | 600 | 0 | 19.385428 | 3.894993 | 否：A 失败 |

index 1 的原始归档再次通过只读独立复算，SPL 为 **0.8251590990**。
其真实 173 帧 A 历史产生 Novel **3 个候选格**、Revisit **2 个候选格**，
不是 B 的导航次数或 B 的成功结果。候选图、位姿、完整实际前缀与查询 JSON 均随该 A 归档。

B 仍须等待全部 144 个 A 状态确定后统一按冻结算法分配，不能先挑这一条的目标运行、再按结果调整。
目前 3 条均来自 `1LXtFkjw3qL`，保留所有成败，不作总体成功率或阶段难度的结论。

### 14:17 CST：六项完成，最终后处理链已提交

新增三个 A 数组项均正常退出并通过各自归档验证：

| A index | Slurm 耗时 | 到达 | 实际路径 (m) | 最终距离 (m) | 两类 B 均可构造 |
|---:|---|---:|---:|---:|---|
| 3 | 3 分 58 秒 | 1 | 4.791423 | 0.989135 | 是 |
| 4 | 3 分 46 秒 | 1 | 2.778986 | 0.981771 | 是 |
| 5 | 5 分 26 秒 | 0 | 20.146939 | 3.153608 | 否：A 失败 |

index 3 已额外用独立后处理器从原始归档复算，SPL 为 1；
index 4/5 此处采用各自完整归档中的结果与 producer verifier，完整总体结束后再统一独立复算。
index 5 同样是 600 步预算内未到达，不是环境故障，不替换目标。
截至本次查询，剩余 `17357681_[6-143%2]` 为 `PENDING (QOSGrpGRES)`，没有正在运行的该数组项。
总计 6 项均来自第一场景 `1LXtFkjw3qL`，因此 3/6 仅为当前采集计数。

为保证结果结束后自动完成独立复算，另外提交了 **CPU 作业 `17360494`**：

```text
A reducer 17357682
    → after_A 17360494：读取实际 B reducer 的 Job ID
    → after_B：等待 B reducer，读取实际 C summary 的 Job ID
    → verify：等待最终汇总，独立读取原始归档，生成完整中文结果报告
```

这部分只有调度和只读后处理，不改变冻结的 GPU 运行时、样本、参数或导航任务。
四个后处理文件另行封存，清单 SHA-256 为
`51d0978fee2d36b39ea67067f451931920acaba84aa540ada1368f7f3a52e810`；
本地文件与提交收据逐一一致。相关 14 项测试和 sbatch 语法检查通过。
对应提交收据为远端结果目录的 `postprocessing_submission.json`，
本地副本在 `.diagnostics/table2_full_rerun_20260911_v2/hpc_receipts/`。

额外核实 Torch 的 `MinJobAge=64 s`：若后处理启动时前序 CPU job 已从 `squeue` 移除，
会依据 `sacct` 确认其 `COMPLETED / 0:0` 后续接；失败或未知状态不会放行。
此经验已加入 HPC 共享 SSH 手册第 19 节，避免把正常清除的历史 job 误当作失效依赖。

当前 **没有 B/C SR，也没有完成新版 Table II**；整个 goal 继续保持 active。

### 14:23 CST：旧 pilot 的 B→C 汇总流程核查

旧 HM3D 8-source pilot 的 reducer `17348184` 已 `COMPLETED / 0:0`，耗时 30 秒，
其 `paired_summary.json` 已生成；不是依赖卡死。它最终没有可运行的 C 查询：
两条成功的 native Novel-B 只能构造 C-Revisit、不能构造 C-Novel，
没有成功的 native Revisit-B，因此 `c_query_tasks=0`。
这里的 `completed=true` 表示该 pilot 已执行完声明的流程，**不表示已获得三阶段证据**；
其 `formal_result=false`，不能纳入新版 Table II。

本次只读核查没有重启或更改旧 pilot。新版 MP3D 144-source 仍在执行，
使用的是在正式查询前已完成容量审计的 18 场景。
容量审计不能替代真实 A/B 的构造供给验收，最终仍须如实报告 C 数量和损耗。

### 14:29 CST：第一场景八项均完成

`17357681_6` 与 `_7` 在 `ga041` / `ga006` 分别耗时 5 分 10 秒 / 4 分 54 秒，
均 `COMPLETED / 0:0`，stderr 为空，完整归档验证通过。

| A index | 已执行步数 | 到达 | 实际路径 (m) | 最终距离 (m) | SPL | 两类 B 均可构造 |
|---:|---:|---:|---:|---:|---:|---|
| 6 | 324 | 1 | 7.613835 | 0.983501 | 0.537489 | 是 |
| 7 | 356 | 1 | 12.608819 | 0.981432 | 0.657086 | 是 |

查询期间曾短暂看到作业已离开队列、收据尚未可见；随后 `sacct` 与归档均确认正常完成。
没有据此重启作业，也没有重复执行这两条导航。
当前完成的是 `1LXtFkjw3qL` 的全部 8 个 A，成功 5 个，5 个成功前缀都具备两类 B 候选。
剩余 `17357681_[8-143%2]` 等待 GPU 配额；B/C 仍等待总体构造阶段完成。

### 14:48 CST：第二场景两条完成，提前终止原因已核对

`17357681_8` / `_9` 均 `COMPLETED / 0:0`，耗时 3 分 58 秒 / 3 分 55 秒，
来自 `1pXnuDYAj8r`。两条均未到达，并非环境或归档失败。

| A index | 已执行步数 | 实际路径 (m) | 最终距离 (m) | 终止原因 | blocked_step_count |
|---:|---:|---:|---:|---|---:|
| 8 | 260 | 8.713562 | 5.502830 | stuck | 9 |
| 9 | 200 | 1.954861 | 2.597468 | stuck | 146 |

单独的只读复算器直接读取两份原始归档，确认末步位置、实际路径、失败标签和 SPL=0 一致。
原始 `actual_trace.json` 均明确记录 `termination_reason="stuck"`；
模型服务日志正常，未见本次提前结束由 HTTP/进程报错触发的证据。

需要明确：600 ticks 是最大预算，不是强制走满。冻结运行时沿用原 evaluator 的停止规则：
历史长度超过 150、该窗口首末平面位移小于 0.10 m 时终止。
这个条件检查的是首末净位移，不表示整个窗口完全没有运动。
它没有在本轮新增或调整；不因看到这两条失败而放宽规则、替换目标或重跑。
两条均计入 A 分母，按协议不继续构造 B。

当次队列中索引 10 在 `ga006` 运行，剩余项受 QOS 调度；
B/C 及最终后处理依赖有效，尚无新 B/C 成绩。

### 15:00 CST：已完成 13 个 A，7 个前缀可构造 B 配对

新增完成项均来自 `1pXnuDYAj8r`，Slurm 均为 `COMPLETED / 0:0`：

| A index | Slurm 耗时 | 已执行步数 | 到达 | 实际路径 (m) | 最终距离 (m) | SPL | 两类 B 均可构造 |
|---:|---|---:|---:|---:|---:|---:|---|
| 10 | 5 分 23 秒 | 600 | 0 | 19.517624 | 4.613148 | 0 | 否：A 失败 |
| 11 | 6 分 20 秒 | 251 | 1 | 7.621199 | 0.985056 | 0.533503 | 是 |
| 12 | 5 分 01 秒 | 203 | 1 | 7.227970 | 0.976120 | 0.401864 | 是 |

没有因第二场景早先三条失败而更改目标、深度条件、controller 或停止规则；
随后两条按原协议执行成功。当前累计 A 到达 7/13，只是未完成总体的采集进度。
当次队列中索引 13、14 分别在 `ga040`、`ga035` 运行，待启动范围为 `[15-143%2]`。

### 15:11 CST：15 项完成，新增提前终止项通过原始归档复算

索引 13 / 14 在 `ga040` / `ga035` 均正常结束，Slurm `COMPLETED / 0:0`，
耗时分别为 5 分 05 秒 / 5 分 18 秒。

| A index | 已执行步数 | 实际路径 (m) | 最终距离 (m) | 到达 | SPL | 终止原因 | blocked_step_count |
|---:|---:|---:|---:|---:|---:|---|---:|
| 13 | 444 | 10.399727 | 12.421161 | 0 | 0 | stuck | 89 |
| 14 | 455 | 10.217867 | 9.259115 | 0 | 0 | stuck | 0 |

独立复算器从原始轨迹、末步动作和落点复算，数值均与归档汇总一致。
索引 14 的 `blocked_step_count=0` 也提醒：该 `stuck` 标签指窗口首末净位移不足，
不能直接解释成全程原地不动或碰撞卡死。没有修改停止条件或删除这些失败。
这两份约 70/71 MB 的归档只读复算分别耗时约 1.07/1.05 秒；这是后处理耗时，不是导航时长。

当前累计 7/15 个 A 到达，7 个成功前缀均可构造 B 两种角色。
索引 15、16 已在 `ga040`、`ga018` 运行，后续数组继续调度。

### 15:18 CST：第二场景完成，开始第三场景

新增两项均正常归档并通过独立原始轨迹复算：

| A index | scene | 已执行步数 | 实际路径 (m) | 最终距离 (m) | 到达 | SPL | 终止原因 |
|---:|---|---:|---:|---:|---:|---:|---|
| 15 | 1pXnuDYAj8r | 422 | 10.018339 | 10.514398 | 0 | 0 | stuck |
| 16 | 29hnd4uzFmX | 600 | 21.995514 | 1.838223 | 0 | 0 | max_steps |

Slurm 耗时为 4 分 41 秒 / 6 分 00 秒，均 `COMPLETED / 0:0`。
前两个完整场景的 A 分别为 5/8 和 2/8；第三场景目前只完成 1 项。
索引 16 的最终距离仍大于冻结的 1 m 成功标准，因此计为失败，没有因接近目标而改记成功。
当前仍有 7 个可构造两类 B 的成功 A 前缀；B/C 要等待全部 A 处理完后统一选择并执行。

### 15:21 CST：19 项完成，首次出现成功但不能构造 B 配对的短历史

新增两项来自 `29hnd4uzFmX`，均正常归档，Slurm `COMPLETED / 0:0`：

| A index | Slurm 耗时 | 已执行步数 | 实际路径 (m) | 最终距离 (m) | 到达 | SPL | 两类 B 均可构造 |
|---:|---|---:|---:|---:|---:|---:|---|
| 17 | 4 分 39 秒 | 309 | 9.795805 | 0.964605 | 1 | 0.721376 | 是 |
| 18 | 3 分 54 秒 | 32 | 1.191073 | 0.982252 | 1 | 1 | 否 |

索引 18 的构造诊断明确为 Novel 菜单 3 格、Revisit 菜单 0 格，
`diagnostics.revisit.source_frames=[]`，没有执行 Revisit 候选检查。
它只有 32 帧实际历史，不满足冻结的历史来源帧采样范围；没有补帧、重复图像或放宽规则。
因此 A 成功仍计入分母和分子，但不能派生本轮要求的 B-Novel/B-Revisit 配对。

已核对后续 selector 的既有逻辑：`select_b` 仅选择 `both_constructed=true` 的前缀；
该来源会在 B 构造损耗中保留，并生成无查询的 CPU 记录，而不是算作 B 导航失败。
这解释了当前 **A 成功 9 条、可构造 B 配对 8 条** 的差额。
所有实际 B 查询仍须等 144 个 A 全部处理完后，统一按候选供给分配。

### 15:27 CST：21 项完成，10 个合格 B 前缀

索引 19、20 正常归档，完整收据与对应 verifier 一致，均成功并可构造 B 配对：

| A index | 已执行步数 | 实际路径 (m) | 最终距离 (m) | 到达 | SPL |
|---:|---:|---:|---:|---:|---:|
| 19 | 263 | 8.674238 | 0.978360 | 1 | 0.925642 |
| 20 | 98 | 3.608289 | 0.983670 | 1 | 1 |

累计 A 成功 11/21，合格 B 前缀 10 个；索引 18 的短历史构造损耗仍完整保留。
当前运行的是索引 21、22，分别位于 `ga002`、`ga010`；其余按既有数组继续调度。

### 15:50 CST：30 项完成，离线验证故障完成 CPU 修复

索引 21 原 GPU 作业 `17357681_21` 在导航结束后，因 verifier 重新加载 NavMesh
产生不同连通区域划分而失败。本机相同 GLB、相同构建参数复现此差异：
保存的 NavMesh 逐字节一致，但现场重建有 2 个区域、重新加载有 3 个。
按执行时的重建方式复算，164/164 个动作的落点完全一致。

单独 CPU 作业 `17365241`（22 秒）随后完成全任务复核，原始归档独立复算再次通过。
旧 `artifacts.tar.gz` 和所有日志移至 `failed_attempts/17357681_21_collision_context/`，
未删除；新完整归档包含补充说明。该来源的 A 结果仍是 0/1，实际路径 1.957327 m，
最终距离 4.151704 m，SPL=0。没有第二次导航，也没有放宽碰撞验证容差。

细节与文件哈希见 `TABLE2_COLLISION_REVERIFICATION_20260911.md`。
主数组保持运行，A reducer 尚未启动，因此无需重提全数组或修改下游计划。
当次核对：30 项完整归档、14 个 A 到达、12 个可构造 B-Novel/B-Revisit 的前缀，
没有尚未解决的归档验证失败。

### 15:56 CST：32 项已归档，后续依赖正常

最新逐项收据检查为 32 项完成、14 个 A 到达、12 个合格 B 前缀，无未解决的验证失败。
本地已同步前 30 项的小型 summary/verification/archive 收据并核对哈希；
补充 CPU 修复和 Table II 后处理相关回归测试合计 69 passed。
主数组继续按两项并发运行，`17357682` 与 `17360494` 仍分别等待前序阶段，
尚未产生 B/C 的正式导航结果。

### 16:03 CST：34 项完成，最新两项原始轨迹复算通过

新增索引 32 / 33 均来自 `2n8kARJN3HM`：

| A index | 步数 | 实际路径 (m) | 最终距离 (m) | 到达 | SPL | 终止原因 |
|---:|---:|---:|---:|---:|---:|---|
| 32 | 600 | 21.416826 | 18.947853 | 0 | 0 | max_steps |
| 33 | 462 | 16.737760 | 14.865352 | 0 | 0 | stuck |

独立读取原始归档中的逐动作落点与最终坐标复算，均与生产汇总一致。
34 项中 A 到达 14 条、合格 B 前缀 12 条；没有新增归档验证失败。
队列曾短暂受 `QOSGrpGRES` 限制为一项运行，随后恢复为两个并发项。
后续仍按全量 A → 全局 B 选择 → 配对 B → 全局 C 选择推进，未提前挑选部分来源运行 B/C。

### 16:05 CST：新增一条短历史 A 成功

索引 35 正常完成，Slurm `COMPLETED / 0:0`、耗时 3 分 33 秒，A 在 34 步到达。
该实际历史未形成合规的两类 B 配对，A 成功照常保留，合格 B 前缀数量仍为 12。
当次累计 35 个完整归档、15 个 A 成功；索引 34 仍在运行，后续项等待 GPU 配额。
此前前 34 项的小型收据已同步本机并通过哈希/验证绑定检查。

### 16:10 CST：36 项完成，成功项的实际 SPL 复算通过

索引 34 正常完成并形成新的两类 B 前缀。对索引 34、35 的原始轨迹独立复算：

| A index | 步数 | 实际路径 (m) | 最终距离 (m) | 到达 | 实际 SPL | B 两种角色可构造 |
|---:|---:|---:|---:|---:|---:|---|
| 34 | 574 | 20.856337 | 0.988965 | 1 | 0.311127 | 是 |
| 35 | 34 | 1.254163 | 0.993632 | 1 | 1 | 否 |

两条均按真实末步坐标和位移积分验证成功；34 的成功不代表路径高效，其 SPL 为 0.311127。
累计 36 个 A 完整归档、16 个到达、13 个 B 配对前缀；前 36 项的小型收据已同步本机并核对。
后续 A 数组继续推进，B/C 仍等待全量 A 汇总。

16:11 补记：索引 36 以 142 步成功完成，归档 `completed=true / exit_code=0`，
且可构造 B 的两种角色。累计 37 项完成、17 个 A 到达、14 个 B 配对前缀；
当前运行索引为 37、38，下游 reducer 和最终复算依赖正常。

### 16:18 CST：仅提升 A 数组并发，未改实验

对同一数组 `17357681` 将 `ArrayTaskThrottle` 从 2 提高为 4；
实际 `scontrol` 及待运行范围 `%4` 已确认。用户 GPU QOS 上限为 16，
分区 QOS 总量上限为 60，实际运行数量仍由调度器决定。

没有重提任务，A100 / 每项 1 GPU / 1 小时 / 10 CPU / 96 GB 均不变，
也没有更改任何样本、方法参数、目标或配对规则。
这是原协议资源段的独立补充，原计划与首次提交收据原样保留。
详细说明见 `TABLE2_RESOURCE_SCHEDULE_ADDENDUM_20260911.md`。

### 16:20 CST：41 项完成，四项并发已实际运行

归档收据逐项核对得到 41 个 A 完成、18 个到达、15 个合格 B 前缀，无新增验证失败。
索引 39 为 587 步失败；索引 40 在 460 步到达，并成功构造两类 B。
调度器已实际运行索引 41、42、43、44 四项，不只是将上限写成 4。
新的调度收据确认 SOURCE_BUNDLE / plan 哈希未变，未重提已完成或正在运行的任务。

### 16:25 CST：44 项完成，17 个合格 B 前缀

新增索引 41 / 42 / 43 均正常完成，分别在 264 / 36 / 416 步到达；
41、43 可构造两类 B，42 的短历史没有形成合规 B 配对。
累计 44 项完成、21 个 A 到达、17 个合格 B 前缀，无未解决的归档验证故障。

原始归档复算确认：索引 39 为 587 步 `stuck` 失败，实际路径 16.749994 m、SPL=0；
索引 40 为 460 步成功，实际路径 16.518570 m、SPL=0.121811。
前 41 项小型收据及调度补充收据已同步本机并验证绑定。

并发上限仍为 4，实际 running 数随完成/调度变化；当次为 44、45 两项运行，
后续项暂受 QOS 限制。不能把“上限 4”解释成始终有 4 张卡。

### 16:29 CST：46 项完成

索引 44 在 455 步成功并形成合格 B 配对；索引 45 为 474 步失败，均完整归档、
生产 verifier 与归档收据一致。累计 A 成功 22/46、合格 B 前缀 18 个，无新增验证故障。
调度器随后再次实际运行四项（46、47、48、49），原数组及下游依赖继续保留。

### 16:39 CST：54 项完成，新增八条原始轨迹复算一致

索引 46–53 均完整归档，其中 47 / 48 / 49 到达并形成合格 B 配对，
其余五项未到达；全部逐动作位置与最终落点经独立读取归档复算，SR/SPL 与生产记录一致。
索引 53 跑满 600 步后最终距离为 1.338460 m，按冻结的 1 m 到达标准仍为失败，未改分。
累计 54/144 个 A 完成、25 个到达、21 个两类 B 可构造前缀，无未解决的验证失败。
当次运行索引为 54、55，后续因 `QOSGrpGRES` 暂未占满四项并发；不是程序异常。
B/C 尚未运行，继续等待完整 A 总体后的全局查询分配。

### 16:50 CST：58 项完成，等待 GPU 配额

前 58 项的小型收据已同步本机，逐项核对 summary 的 SHA、生产 verifier、
完整归档校验标志及任务身份，全部通过。新增索引 54–57 的原始动作归档独立复算也一致：
其中 56 在 85 步到达，SPL=1，但没有形成完整 B 角色对；其余三项为失败。
索引 57 的最终距离为 1.079348 m，按原标准仍为失败，未修改到达阈值。

当前 A 成功 26/58，合格 B 前缀 21 个，无未解决的验证失败。
16:48–16:50 查询时暂无实际 running 项，剩余 `58-143%4` 为 `PENDING (QOSGrpGRES)`；
这是分区 GPU 配额等待，不是数组取消、环境失败或 SSH 失效。保留同一数组继续排队。
B 选择 reducer `17357682` 和后处理续接 `17360494` 均正常等待依赖，尚无 B/C 正式 SR。

16:52 补记：调度已恢复，索引 58 在 A100 节点 `ga021` 开始运行；
其余 `59-143%4` 继续等待配额。未更换数组、容器、controller 或已封存的查询。

### 16:59 CST：59 项完成，两项继续运行

索引 58 完整归档，600 步未到达；独立积分路径为 18.992548 m，最终距离 12.010167 m，
SPL=0，与生产记录一致。对应 summary / verification / archive 收据已同步并核对绑定。
累计 59 项完成、26 个 A 成功、21 个合格 B 前缀，无未解决验证错误。
最新 Slurm 实际运行索引 59、60，后续仍因 GPU 配额等待；全量 A 和后续 B/C 链继续，
没有重启已完成任务或修改任何科学条件。本轮属于新增归档验证与对活跃作业的已核实等待。

### 17:11 CST：63 项完成，新归档复算与本机收据绑定通过

新增索引 59–62 中，59 / 62 到达并形成两类 B，60 / 61 以 `stuck` 结束。
四项均通过原始逐动作/末步落点独立复算；两条成功轨迹的 SPL 分别为
0.548909（实际路径 16.040996 m）和 0.335460（实际路径 13.208464 m）。
前 63 项的小型收据已全部同步本机并核对 summary SHA、归档校验标志和任务身份。

累计 A 成功 28/63，合格 B 前缀 23 个，无未解决验证失败。
当前索引 63、64 在运行，余下任务等待配额；B/C 仍未开始，全量阶段间依赖保持正常。
监控明确区分归档任务数与实际非空查询数，后续 CPU 产生的空 B 记录不会计入导航分母。

### 19:37 CST：A 全量完成，B 已自动提交

直接核对远端全量收据：144 个 A 均 `completed=true`，生产 verifier 与归档绑定一致，
没有未解决的验证失败。A 到达 50/144（34.72%）；其中 38 个实际前缀同时可构造 B-Novel / B-Revisit。
另外 94 个 A 未到达、12 个成功 A 没有完整 B 角色对，均保留为流转损耗。

`b_population.json` 已汇总全部 144 个 A summary，记录 `B_navigation_outcomes_read=false`，
选择 38 个共享前缀、76 个查询，后续每个查询运行 native/GEM，共 152 次导航。
106 个未纳入来源对应 212 份 CPU 空 B 记录；它们没有执行导航，不能计入 B 的 SR 分母。
全量原始归档的独立统计复算仍由最终后处理执行，本次不是宣称最终全链审计已经完成。

| 当前阶段 | Job ID | 实查状态 |
|---|---|---|
| 全 A 汇总 / B 选择 | `17357682` | COMPLETED / 0:0，56 秒 |
| A 后处理续接 | `17360494` | COMPLETED / 0:0，17 秒 |
| B 首个配对任务 | `17367918_[2]` | PENDING / QOSGrpGRES |
| 其余 B 查询 | `17367919` | PENDING / Dependency |
| B 汇总 / C 选择 | `17367920` | PENDING / Dependency |
| B 后处理续接 | `17367925` | PENDING / Dependency |

B 提交仍绑定原 plan 和 source-bundle SHA，单项 1 小时、并发 2。
截至查询时 B 的实际非空导航记录为 0、C 为 0；暂无 native/GEM 新版 Table II 配对 SR。
本轮 A 数值是共同构造下的 native 采集结果，不能与旧 A 跨总体相减，也不能据此断言某一失败原因。
